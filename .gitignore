# Build artifacts
libgstspotify.so
*.so

gst-plugins-rs-build/gst-plugins-rs/target/
# npm/cargo-style target folders just in case
**/target/

# Caches
.cache/

# Local helper scripts (not part of published repo)
publish-repo.sh
